# 基于多模态视频流与自然语言检索的智能取证系统 V1.0

## 软件设计说明书

---

**软件名称：** 基于多模态视频流与自然语言检索的智能取证系统

**软件简称：** 智能视频取证系统

**版本号：** V1.0

**开发完成日期：** 2026年5月

---

## 一、软件概述

### 1.1 软件简介

基于多模态视频流与自然语言检索的智能取证系统（以下简称"本系统"）是一款面向交通场景的视频智能检索与事件取证软件。系统接收交通监控视频作为输入，通过视频抽帧、多模态特征提取、向量索引、事件检测等处理流程，将非结构化的视频数据转化为可检索的结构化资产。用户以自然语言（中文或英文）发起查询，系统融合视觉语义、文字识别、目标检测、轨迹追踪、事件检测等多路信号，返回段级（segment-level）的检索结果，支持交通违法事件的自动化发现与取证。

### 1.2 设计目标

本系统解决传统视频取证中"看不完、找不到、说不清"三个核心痛点。一段 60 秒的交通视频包含约 1800 帧，人工逐帧排查耗时极大；当视频库规模扩大到数十乃至上百路时，人工检索完全不可行。本系统通过以下机制应对该问题：

- 将视频帧转化为 512 维 CLIP 视觉向量并建立 FAISS 索引，使"一辆白色轿车通过路口"这类自然语言描述能直接定位到对应帧
- 对每帧运行 YOLOv8 目标检测，将"car""truck""traffic light"等结构化标签写入元数据，支持精确标签检索
- 跨帧追踪同一目标形成轨迹，推断运动方向，支持"从左向右行驶的车辆"这类运动学查询
- 通过 HSV 色彩空间分析红绿灯状态，结合车辆越线轨迹，自动识别闯红灯事件
- 将以上多路信号在视频时间轴上按 5 秒窗口聚合，输出段级结果而非帧级结果，避免相邻帧重复刷屏

### 1.3 适用场景

- 交通监控视频的违法事件筛查与取证
- 特定车辆、行人、交通设施的快速定位
- 交通事故过程的段级回溯
- 视频证据的结构化标注与检索

---

## 二、运行环境

### 2.1 硬件环境

| 项目 | 最低配置 | 推荐配置 |
|------|----------|----------|
| CPU | x86_64，4核 | x86_64，8核以上 |
| 内存 | 8 GB | 16 GB 以上 |
| 存储 | 20 GB 可用空间 | 50 GB 以上（含视频库） |
| GPU | 无（CPU 推理） | 可选，NVIDIA GPU 加速 CLIP/YOLO 推理 |

### 2.2 软件环境

| 项目 | 版本要求 |
|------|----------|
| 操作系统 | Windows 10/11，或 Linux（Ubuntu 20.04+） |
| Python | 3.10 及以上 |
| 包管理 | pip 24.0 及以上 |

### 2.3 主要依赖库

| 依赖库 | 版本 | 用途 |
|--------|------|------|
| fastapi | ≥0.115.0 | Web 服务框架 |
| uvicorn | ≥0.30.0 | ASGI 服务器 |
| opencv-python | ≥4.10.0 | 图像处理与视频抽帧 |
| open-clip-torch | ≥2.32.0 | 视觉-语言预训练模型 |
| faiss-cpu | 1.13.2 | 向量近邻检索 |
| torch | ≥2.4.0 | 深度学习推理后端 |
| paddleocr | ≥2.8.1 | OCR 文字识别 |
| paddlepaddle | ≥3.0.0 | PaddleOCR 推理后端 |
| ultralytics | ≥8.3.0 | YOLOv8 目标检测 |
| rapidfuzz | ≥3.9.6 | 模糊字符串匹配 |
| pydantic | ≥2.8.0 | 数据模型校验 |
| jinja2 | ≥3.1.4 | Web 模板渲染 |
| numpy | ≥1.26.4 | 数值计算 |
| pillow | ≥10.4.0 | 图像文件读写 |

---

## 三、安装与部署

### 3.1 获取软件

软件源代码部署于本地工作目录，目录结构如下：

```
project/
├── main.py                  # FastAPI 应用入口
├── config.py                # 全局配置
├── requirements.txt         # 依赖清单
├── yolov8n.pt               # YOLOv8 预训练权重
├── api/                     # API 路由层
│   └── routes.py
├── core/                    # 应用核心
│   ├── app_state.py         # 服务容器
│   └── schemas.py           # 数据模型定义
├── services/                # 业务服务层
│   ├── clip_service.py
│   ├── search_service.py
│   ├── index_service.py
│   ├── ocr_service.py
│   ├── detection_service.py
│   ├── tracking_service.py
│   ├── event_service.py
│   ├── hybrid_search_service.py
│   └── ...
├── configs/events/          # 事件插件配置
├── videos/                  # 源视频目录
├── frames/                  # 抽取帧目录
├── embeddings/              # CLIP 嵌入存储
├── index/                   # FAISS 索引
└── metadata/                # 结构化元数据
    ├── detections/
    ├── trajectories/
    ├── events/
    └── ocr/
```

### 3.2 安装依赖

在项目根目录执行以下命令安装全部依赖：

```bash
pip install -r requirements.txt
```

首次运行时，OpenCLIP 会自动下载 ViT-B-32 预训练权重（约 350 MB），PaddleOCR 会自动下载检测与识别模型。

### 3.3 启动服务

```bash
python main.py
```

服务启动后监听 `0.0.0.0:8000`，提供以下访问入口：

- Web 检索界面：`http://localhost:8000/`
- API 文档（Swagger）：`http://localhost:8000/docs`
- 帧图片静态服务：`http://localhost:8000/frames/<filename>`

服务启动时会自动创建 `videos`、`frames`、`embeddings`、`index`、`metadata` 等工作目录。

---

## 四、系统架构

### 4.1 分层结构

系统采用分层架构，自上而下分为四层：

**表示层** —— 基于 FastAPI + Jinja2 的 Web 界面，提供自然语言检索输入框、结果展示、帧图片预览。同时暴露 RESTful API 供外部程序调用。

**API 路由层** —— `api/routes.py` 定义全部 HTTP 端点，负责请求校验、服务依赖注入、异常转译。包含入库类端点（视频入库、检测入库、OCR 入库、轨迹入库、事件入库）和检索类端点（CLIP 检索、OCR 检索、检测检索、轨迹检索、事件检索、混合检索）。

**业务服务层** —— `services/` 目录下 18 个服务模块，每个服务封装一类业务逻辑。服务之间通过 `core/app_state.py` 的 `AppState` 容器装配为单例，共享整个应用生命周期。

**数据层** —— 以本地 JSON 文件为元数据载体，以 `.npy` 文件存储 CLIP 嵌入向量，以 FAISS 索引文件存储向量近邻索引。所有元数据按视频粒度组织，文件名以视频内容的稳定哈希命名，确保重命名不影响检索。

### 4.2 服务容器

`AppState` 是应用级服务容器，在 FastAPI 启动时通过 `AppState.build(settings)` 一次性构造全部 16 个服务实例并注入依赖关系。这种设计避免每请求重建重量级对象（如 CLIP 模型、PaddleOCR 模型、FAISS 索引），同时保持服务间的显式依赖声明。核心装配关系如下：

- `ClipService` 接收 `Settings`，加载 OpenCLIP 模型
- `IndexService` 接收 `Settings`，管理 FAISS 索引的加载、追加、持久化
- `SearchService` 接收 `Settings`、`ClipService`、`IndexService`，执行文本到帧的向量检索
- `HybridSearchService` 接收查询改写、CLIP 检索、OCR 检索、检测检索、轨迹检索、事件检索六个子服务，以及结果聚合服务，执行多模态融合检索

---

## 五、功能模块说明

### 5.1 视频入库与帧提取

**模块文件：** `services/video_service.py`、`services/frame_extractor.py`

**功能描述：** 接收视频文件路径，按配置的帧间隔（默认每 30 帧取 1 帧）抽取视频帧为 JPEG 图片，保存至 `frames/` 目录。帧文件名采用"视频名\_时间戳.jpg"格式，时间戳精确到 0.1 秒，便于后续检索结果直接定位到视频时刻。支持单视频入库和目录批量入库两种模式。

**处理流程：**

1. 调用 OpenCV 的 `VideoCapture` 打开视频文件，读取总帧数与帧率
2. 按帧间隔跳帧读取，对每一帧计算时间戳（帧号 ÷ 帧率）
3. 将帧写入 JPEG 文件，文件名编码视频名与时间戳
4. 对每个抽取帧调用 CLIP 服务生成 512 维视觉嵌入向量
5. 将嵌入向量追加到 FAISS 索引，帧元数据（视频名、时间戳、帧路径）写入 `index/frame_metadata.json`

**API 端点：**

- `POST /api/videos/ingest` —— 单视频入库
- `POST /api/videos/ingest-directory` —— 目录批量入库

### 5.2 CLIP 语义检索

**模块文件：** `services/clip_service.py`、`services/search_service.py`、`services/index_service.py`、`services/faiss_index_service.py`、`services/embedding_service.py`

**功能描述：** 基于 OpenCLIP ViT-B-32 预训练模型，将自然语言查询编码为 512 维文本向量，与视频帧的视觉向量在统一嵌入空间中计算余弦相似度，通过 FAISS 索引快速检索最相似的帧。该模块支持中英文查询，但中文查询建议经查询改写模块（5.8 节）转写为英文以提升精度。

**技术实现：**

- `ClipService` 封装 OpenCLIP 模型的加载与推理，提供 `encode_text`（文本编码）和 `encode_image`（图像编码）两个核心方法
- `FaissIndexService` 管理 FAISS `IndexFlatIP` 索引（内积相似度，向量预先 L2 归一化），支持索引的创建、追加、搜索、持久化
- `IndexService` 在 `FaissIndexService` 之上维护帧元数据的映射关系，确保检索结果能还原到视频名、时间戳、帧路径
- `SearchService` 接收查询文本，调用 `ClipService` 编码为向量，经 `IndexService` 检索 top-k 帧，按相似度降序返回

**API 端点：** `POST /api/search`

**请求示例：**

```json
{ "query": "a white car crossing the intersection", "top_k": 5 }
```

### 5.3 OCR 文字检索

**模块文件：** `services/ocr_service.py`、`services/ocr_search_service.py`、`services/ocr_metadata_store.py`

**功能描述：** 基于 PaddleOCR 对视频帧进行文字识别，提取帧内出现的车牌号、路牌、指示牌等文字信息，建立文字到帧的倒排索引。用户输入文字查询时，通过 RapidFuzz 模糊匹配定位包含相似文字的帧。

**处理流程：**

1. `OCRService` 懒加载 PaddleOCR 模型（首次调用时初始化，后续复用单例）
2. 对指定目录下的全部帧逐张运行 OCR，每帧产出多个文字区域（含文本内容、置信度、四点边界框）
3. 按"视频名\_帧时间戳"粒度组织 OCR 元数据，写入 `metadata/ocr/<video_id>.json`
4. 检索时，`OCRSearchService` 遍历全部 OCR 元数据，对每个文字区域与查询计算 RapidFuzz 相似度，按相似度降序返回 top-k 结果

**API 端点：**

- `POST /api/ocr/ingest-directory` —— OCR 元数据批量生成
- `POST /api/ocr/search` —— OCR 文字检索

### 5.4 目标检测检索

**模块文件：** `services/detection_service.py`、`services/detection_search_service.py`

**功能描述：** 基于 Ultralytics YOLOv8n 模型对视频帧进行交通目标检测，识别 `car`、`truck`、`bus`、`motorcycle`、`person`、`traffic light` 六类交通相关目标，将检测框、标签、置信度写入元数据。用户可按标签精确检索包含特定目标的帧。

**技术实现：**

- `DetectionService` 懒加载 YOLOv8n 权重（`yolov8n.pt`），对帧目录批量推理
- 检测结果按视频粒度组织，每帧包含多个 `DetectionItem`（标签、置信度、bbox 坐标）
- `DetectionSearchService` 接收目标标签查询，经查询改写模块将中文标签（如"轿车"）映射为英文标签（如"car"），遍历检测元数据返回匹配帧
- 支持按置信度排序，可配置最低置信度阈值（默认 0.25）

**API 端点：**

- `POST /api/detection/ingest-directory` —— 检测元数据批量生成
- `POST /api/detection/search` —— 目标检测检索

### 5.5 轨迹追踪检索

**模块文件：** `services/tracking_service.py`、`services/trajectory_search_service.py`

**功能描述：** 在检测元数据基础上，跨帧关联同一目标的检测框形成轨迹（track），计算每条轨迹的起止时间、持续时间、平均置信度、运动方向。用户可按目标标签、运动方向、最小时长检索轨迹。

**技术实现：**

- `TrackingService` 读取检测元数据，对同一视频内相同标签的检测框按时间顺序关联
- 每条轨迹包含有序的 `TrajectoryPoint` 序列（时间戳、帧路径、bbox、中心坐标、置信度）
- 运动方向通过轨迹首尾中心点的位移向量推断，映射为 `left_to_right`、`right_to_left`、`top_to_bottom`、`bottom_to_top`、`unknown` 五种方向
- `TrajectorySearchService` 支持按标签、方向、最小时长三维过滤，返回代表帧与轨迹摘要

**API 端点：**

- `POST /api/tracking/ingest-directory` —— 轨迹元数据批量生成
- `POST /api/trajectory/search` —— 轨迹检索

### 5.6 事件检测与检索

**模块文件：** `services/event_service.py`、`services/event_search_service.py`、`services/event_plugins/red_light_violation.py`、`services/event_plugins/vehicle_crosses_line.py`

**功能描述：** 系统采用插件化事件检测架构。事件插件接收一个视频的检测元数据与轨迹元数据，结合插件配置（如停止线坐标、允许的车辆类型），输出事件记录。当前实现两个事件插件：

**车辆越线事件（vehicle_crosses_line）：**

- 配置一条停止线（两点坐标）
- 遍历每条轨迹，用有符号距离判定轨迹中心点是否穿越该线
- 越线时刻前后各取一帧作为证据帧，输出事件记录

**闯红灯事件（red_light_violation）：**

- 在越线检测基础上，增加红绿灯状态判定
- 从检测元数据中提取越线时刻前后 2 秒窗口内的 `traffic light` 检测框
- 裁剪红绿灯区域，转 HSV 色彩空间，按上、中、下三段分别统计红色（Hue 0-10 或 160-180）、黄色（Hue 15-40）、绿色（Hue 40-95）像素占比
- 三段中红色得分最高且超过阈值（默认 0.01）时，判定为红灯
- 事件置信度 = 轨迹平均置信度 × 0.6 + 红绿灯置信度 × 0.4

**事件检索：**

`EventSearchService` 接收事件类型查询（如"red_light_violation""闯红灯""red light"），经查询改写后匹配事件类型别名，返回事件记录（含起止时间、轨迹 ID、代表帧、证据帧、结构化属性）。

**API 端点：**

- `POST /api/event/ingest-directory` —— 事件元数据批量生成
- `POST /api/event/search` —— 事件检索

### 5.7 混合多模态检索

**模块文件：** `services/hybrid_search_service.py`、`services/result_aggregation_service.py`、`services/segment_service.py`

**功能描述：** 混合检索是系统的核心检索能力。它接收一条自然语言查询，经查询改写后并行调用 CLIP 检索、OCR 检索、检测检索、轨迹检索、事件检索五路子服务，将各路结果在视频时间轴上按 5 秒窗口聚合为段级结果，按查询意图自适应加权融合，输出去重后的段级检索结果。

**融合算法：**

1. **查询意图解析** —— `QueryRewriteService` 将查询分类为 `object`（目标）、`motion`（运动）、`relational`（关系）、`composite`（复合）、`event`（事件）、`semantic`（语义）六种意图类型
2. **并行检索** —— 根据意图类型决定调用哪些子服务。例如 `event` 意图跳过 CLIP 检索，主要依赖事件检索
3. **段级聚合** —— 各路结果按"视频 ID + 5 秒时间桶"为键合并到同一段。同一段内多路命中的结果合并 `matched_by` 字段，记录哪些模态贡献了该段
4. **意图自适应加权** —— 不同意图类型赋予不同模态权重：

   | 意图类型 | CLIP | 检测 | 轨迹 | 事件 |
   |----------|------|------|------|------|
   | object | 0.15 | 0.50 | 0.20 | 0.15 |
   | motion | 0.10 | 0.20 | 0.55 | 0.15 |
   | relational | 0.10 | 0.55 | 0.20 | 0.15 |
   | composite | 0.05 | 0.30 | 0.35 | 0.30 |
   | event | 0.05 | 0.10 | 0.15 | 0.70 |
   | semantic | 0.70 | 0.15 | 0.10 | 0.05 |

5. **共识加分** —— 同一段被多个模态命中时给予共识奖励（每多一个模态 +0.05），命中查询中的标签或方向时额外加分
6. **结果排序与截断** —— 按融合分数降序排序，过滤低于阈值（默认 0.2）的结果，截取 top-k 返回

**API 端点：** `POST /api/search/hybrid`

**请求示例：**

```json
{ "query": "闯红灯的车辆", "top_k": 20 }
```

### 5.8 查询意图解析与改写

**模块文件：** `services/query_rewrite_service.py`

**功能描述：** 将用户的自然语言查询解析为结构化意图，并生成检索友好的改写变体。该模块是混合检索的前置组件，直接决定调用哪些子服务及融合权重。

**技术实现：**

系统内置一个交通领域本体（ontology），包含实体（车辆、行人、交通设施）、关系（附近、穿过、进入、离开）、方向（左到右、右到左）、运动（左转、右转、停车）、属性（灯态、时长、颜色）五类语义元素。查询改写流程如下：

1. 对查询文本执行正则匹配，识别中文别名并映射为标准英文实体（如"轿车"→`car`、"红绿灯"→`traffic_light`）
2. 识别方向词、运动词、关系词，填充 `QueryIntent` 结构
3. 识别事件类型（如"闯红灯"→`red_light_violation`）
4. 基于识别结果生成改写查询列表，供 CLIP 检索使用英文变体提升精度

`QueryIntent` 数据模型包含：`query_type`（意图类型）、`primary_entities`（主检索目标）、`context_entities`（上下文实体）、`relations`（关系）、`directions`（方向）、`motions`（运动）、`event_types`（事件类型）、`attributes`（属性）、`rewritten_queries`（改写变体）。

### 5.9 Web 界面与 API 服务

**模块文件：** `main.py`、`api/routes.py`

**功能描述：** 系统提供 Web 界面与 RESTful API 两类访问方式。

**Web 界面：** 基于 Jinja2 模板渲染的检索控制台，首页提供自然语言查询输入框、top-k 选择器、检索结果列表。每条结果显示视频名、时间戳、相似度分数、帧图片预览，帧图片通过 FastAPI 静态文件服务直接加载。

**RESTful API：** 全部端点挂载在 `/api` 前缀下，按功能分为四组：

- **入库类：** 视频入库、目录批量入库、StreetScene 数据集入库、检测入库、OCR 入库、轨迹入库、事件入库
- **检索类：** CLIP 检索、OCR 检索、检测检索、轨迹检索、事件检索、混合检索
- **运维类：** 健康检查（`/api/health`）、索引统计（`/api/index/stats`）
- **页面类：** 首页（`/`）、兼容性检索（`/search`）

API 采用 Pydantic 模型定义请求与响应结构，通过 FastAPI 自动生成 OpenAPI 文档（`/docs`），支持在线调试。全局启用 CORS 中间件，允许跨域访问。

---

## 六、开源软件说明

依据 2026 年软件著作权登记审核要求，本系统使用的第三方开源软件及协议声明如下：

| 开源软件 | 版本 | 开源协议 | 使用范围 |
|----------|------|----------|----------|
| FastAPI | ≥0.115.0 | MIT License | Web 服务框架，用于 API 路由与中间件 |
| uvicorn | ≥0.30.0 | BSD-3-Clause | ASGI 服务器，用于运行 FastAPI 应用 |
| OpenCV | ≥4.10.0 | Apache 2.0 | 图像处理与视频抽帧 |
| open-clip-torch | ≥2.32.0 | MIT License | OpenCLIP 视觉-语言预训练模型，用于 CLIP 检索 |
| PyTorch | ≥2.4.0 | BSD-3-Clause | 深度学习推理后端，支撑 OpenCLIP 与 YOLOv8 推理 |
| faiss-cpu | 1.13.2 | MIT License | 向量近邻检索，用于 CLIP 嵌入索引 |
| PaddleOCR | ≥2.8.1 | Apache 2.0 | OCR 文字识别，用于视频帧文字提取 |
| PaddlePaddle | ≥3.0.0 | Apache 2.0 | PaddleOCR 推理后端 |
| ultralytics (YOLOv8) | ≥8.3.0 | AGPL-3.0 | YOLOv8 目标检测，用于交通目标检测。本系统仅调用其推理接口，未修改其源码 |
| rapidfuzz | ≥3.9.6 | MIT License | 模糊字符串匹配，用于 OCR 检索相似度计算 |
| pydantic | ≥2.8.0 | MIT License | 数据模型校验，用于 API 请求/响应结构与元数据模型 |
| Jinja2 | ≥3.1.4 | BSD-3-Clause | 模板引擎，用于 Web 界面渲染 |
| numpy | ≥1.26.4 | BSD-2-Clause | 数值计算 |
| Pillow | ≥10.4.0 | MIT-CMU License | 图像文件读写 |

**声明：** 本系统自身的业务代码（`api/`、`core/`、`services/`、`utils/` 目录下的全部 Python 源文件）为自主开发，未包含上述开源软件的源码修改。上述开源软件均以独立依赖形式通过 pip 安装引用，本系统不重新分发其源码。YOLOv8 预训练权重 `yolov8n.pt` 由 Ultralytics 官方发布，本系统仅用于推理。

---

## 七、操作流程说明

### 7.1 视频数据入库

1. 将交通监控视频文件（支持 `.avi`、`.mp4` 等格式）放入 `videos/` 目录
2. 调用 `POST /api/videos/ingest-directory`，系统自动抽帧、生成 CLIP 嵌入、建立 FAISS 索引
3. 调用 `POST /api/detection/ingest-directory`，对抽取帧运行 YOLOv8 检测，写入检测元数据
4. 调用 `POST /api/tracking/ingest-directory`，基于检测元数据生成轨迹元数据
5. 调用 `POST /api/event/ingest-directory`，基于检测与轨迹元数据运行事件插件，生成事件元数据
6. （可选）调用 `POST /api/ocr/ingest-directory`，对帧运行 PaddleOCR，生成 OCR 元数据

### 7.2 自然语言检索

1. 访问 `http://localhost:8000/` 打开 Web 检索界面
2. 在查询输入框输入自然语言，如"白色轿车通过路口""从左向右行驶的卡车""闯红灯的车辆"
3. 选择返回结果数量（top-k），点击检索
4. 系统返回段级结果，每条结果显示视频名、时间段、相似度分数、代表帧图片
5. 点击帧图片可查看完整尺寸

### 7.3 事件取证

1. 确保已完成 7.1 节的入库流程（含事件入库）
2. 调用 `POST /api/event/search`，查询 `red_light_violation` 或"闯红灯"
3. 系统返回事件记录，含起止时间、轨迹 ID、置信度、代表帧、证据帧列表
4. 通过代表帧与证据帧定位视频中的违法时刻，完成取证

---

## 八、技术特点

**多模态融合检索** —— 系统不是单一检索引擎，而是将视觉语义（CLIP）、文字识别（OCR）、目标检测（YOLO）、轨迹追踪、事件检测五路信号融合的检索系统。各路信号在视频时间轴上按段聚合，按查询意图自适应加权，避免单一模态的盲区。

**意图感知的检索路由** —— 查询改写模块将自然语言解析为六种意图类型，不同意图触发不同的子服务组合与融合权重。"闯红灯的车辆"会被识别为 `event` 意图，主要依赖事件检索；"白色轿车"被识别为 `object` 意图，主要依赖检测检索。

**段级结果聚合** —— 检索结果按 5 秒时间窗口聚合，同一窗口内多帧命中只保留最高分代表帧，避免相邻帧重复刷屏，输出人类可读的段级结果。

**插件化事件检测** —— 事件检测采用插件架构，新事件类型只需实现 `EventPluginBase` 接口并注册，无需修改核心代码。插件配置独立为 JSON 文件，可按场景定制停止线坐标、车辆类型、置信度阈值等参数。

**元数据驱动设计** —— 全部中间产物（检测框、轨迹、事件、OCR 结果）以 JSON 文件持久化，按视频粒度组织。检索时直接读取元数据，无需重新推理，支持离线分析与跨工具协作。

**单机轻量部署** —— 系统不依赖数据库、不依赖消息队列、不依赖容器编排，全部数据以文件形式存储于本地目录。一台普通工作站即可完成部署，适合单开发者运维与 PoC 验证场景。

---

*本说明书所述功能均与系统源代码实现对应，代码位于 `project/` 目录下。*
